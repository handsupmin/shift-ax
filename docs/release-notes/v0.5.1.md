# v0.5.1

- Added `shift-ax eval` and `npm run eval:objective` for objective request-to-commit metrics.
- The objective eval report covers context retrieval, ambiguity scoring, token reduction, deterministic harness behavior, review gate accuracy, and artifact completeness.
- The eval command writes machine-readable JSON and Markdown reports and exits non-zero when any metric misses its threshold.
