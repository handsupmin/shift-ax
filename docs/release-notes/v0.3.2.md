# Shift AX v0.3.2

## Highlights

- redesigned onboarding into a staged deep-interview flow that asks one thing at a time instead of collecting everything in one burst
- made onboarding inspect referenced repositories before asking for corrections, then present a 1/2/3 confirmation checkpoint with inferred role, layers, focus paths, and hidden conventions
- persisted hidden conventions and architecture-intent notes into generated procedure docs
- strengthened Codex and Claude onboarding scaffold guidance so product-shell sessions keep pushing until role/work/repo/glossary uncertainty is resolved
- made overwrite confirmation friendlier for Korean onboarding responses by accepting the same affirmative variants used elsewhere in the interview

## Verification

- `./node_modules/.bin/tsx --test tests/guided-onboarding.test.ts tests/context-onboarding-cli.test.ts tests/platform-scaffold.test.ts`
- `npm run build`
- `npm test`
- manual smoke: interactive `node --import tsx scripts/ax-onboard-context.ts --root <temp-repo>`
