# v0.5.0

- local SQLite topic state 기반 deterministic orchestration harness를 추가했습니다.
- 실행을 script-owned FSM transition, DAG task readiness, priority queue claim, retry scheduling, idempotency key를 통해 라우팅합니다.
- finite-state transition, DAG readiness, priority ordering, retry delay 알고리즘을 검증하는 dependency-free Rust core crate를 추가했습니다.
- harness가 task를 claim한 뒤 등록된 executor를 통해서만 agent가 실행되도록 execution orchestration을 갱신했습니다.
