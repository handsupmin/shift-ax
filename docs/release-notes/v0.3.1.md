# Shift AX v0.3.1

## Highlights

- localized generated auto-commit titles and bodies from the saved Shift AX language
- kept Lore protocol trailer keys stable in English so validation and downstream tooling still work
- tightened Codex and Claude Code review/resume command guidance so green review flows continue through finalization instead of stalling before the commit

## Verification

- `node --import tsx --test tests/finalization-lore-protocol.test.ts tests/finalization-commit.test.ts tests/request-pipeline-cli.test.ts tests/platform-scaffold.test.ts`
- `npm run build`
- `npm test`
