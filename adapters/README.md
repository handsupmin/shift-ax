# Adapters

The `adapters/` layer connects Shift AX core logic to platform-specific execution surfaces.

This layer exists to keep `core/` portable while letting each platform expose its own runtime mechanics.
