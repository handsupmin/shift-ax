# Core Topics

`core/topics` is for request-scoped workspace artifacts.

This layer should define:

- topic slugging
- topic directory structure
- request logging
- request summary artifacts
- worktree-related metadata
