# Core Context

`core/context` is for document and policy resolution.

This layer should answer:

- what index document should be consulted first
- which detailed documents are relevant for the current request
- what evidence was loaded before planning or implementation started
